//var chalk = require('chalk');

// *The above line of code was in regular JavaScript ES5
// This, on the other hand, is ES6
// If the .babelrc file works correctly, we should be fine
import chalk from 'chalk'

console.log(chalk.green('Starting app in dev mode...'));


